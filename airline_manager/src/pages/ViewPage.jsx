import { Center } from '../components/Styles';
import { ViewTable } from '../components/ViewTable';

export const ViewPage = () => {
    return(
        <Center>
            <ViewTable />
        </Center>
    );
}